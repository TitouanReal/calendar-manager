using Gtk 4.0;

template $CalendarComboRowHeader: Box {
  margin-start: 12;
  margin-end: 12;

  Label {
    ellipsize: end;
    xalign: 0.0;
    label: bind template.collection as <$Collection>.name;
  }
}
