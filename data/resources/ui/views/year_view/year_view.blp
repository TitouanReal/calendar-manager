using Gtk 4.0;

template $YearView: Widget {
  overflow: hidden;

  EventControllerScroll {
    flags: vertical | kinetic;
    scroll => $scroll(template);
    decelerate => $decelerate(template);
  }
}
